name: API Test Automation with Enhanced Reporting

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run nightly at 2 AM UTC for full regression
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run tests against'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod

# Permissions set at job level for security (principle of least privilege)

# Global environment variables
env:
  TIMESTAMP: ${{ github.run_id }}

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 1: CODE QUALITY (runs in parallel)
  # ═══════════════════════════════════════════════════════════════════════════
  
  code-quality:
    name: 🎨 Code Quality
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.12'
      
      - name: 📦 Install Ruff
        run: pip install ruff
      
      - name: 🔍 Run Ruff Linter
        run: |
          echo "## 🎨 Code Quality Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          ruff check . --output-format=github || true
          ruff check . --statistics >> $GITHUB_STEP_SUMMARY 2>&1 || true
      
      - name: 🎯 Run Ruff Formatter Check
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 Formatting Check" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Run ruff format check and capture output
          FORMAT_OUTPUT=$(ruff format --check . 2>&1) || true
          
          # Check if any files need formatting
          if echo "$FORMAT_OUTPUT" | grep -q "Would reformat"; then
            echo "⚠️ **Some files need formatting:**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "$FORMAT_OUTPUT" | grep "Would reformat" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "💡 Run \`ruff format .\` to fix formatting issues." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ All files are properly formatted!" >> $GITHUB_STEP_SUMMARY
          fi

  type-check:
    name: 📊 Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.12'
      
      - name: 📦 Install dependencies
        run: |
          pip install mypy types-requests
          pip install -r requirements.txt
      
      - name: 🔍 Run MyPy Type Check
        run: |
          echo "## 📊 Type Check Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Run mypy and capture output
          MYPY_OUTPUT=$(mypy . --ignore-missing-imports 2>&1) || true
          ERROR_COUNT=$(echo "$MYPY_OUTPUT" | grep -c "error:" || echo "0")
          
          if [ "$ERROR_COUNT" -eq "0" ] || [ -z "$ERROR_COUNT" ]; then
            echo "### 🎉 Congratulations! No type errors found" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ All type annotations are correct and consistent" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🔍 Your code passes all type checks with flying colors!" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ Found $ERROR_COUNT type error(s)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "$MYPY_OUTPUT" | grep "error:" | head -20 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
        continue-on-error: true

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 2: BUILD
  # ═══════════════════════════════════════════════════════════════════════════
  
  build:
    name: 🔨 Build & Setup
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
      fail-fast: false
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🐍 Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps chromium
      
      - name: 💾 Cache dependencies
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: |
            ~/.cache/pip
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-pip-playwright-${{ hashFiles('requirements.txt') }}-${{ matrix.python-version }}
          restore-keys: |
            ${{ runner.os }}-pip-playwright-
      
      - name: 📝 Create .env file
        run: |
          cat > .env << EOF
          ENV=${{ github.event.inputs.environment || 'dev' }}
          BASE_URL=https://eks-dev-lb.shadhinlab.xyz
          RETRY_COUNT=2
          PARALLEL_WORKERS=4
          REPORT_PORTAL_ENABLED=false
          EOF
      
      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: build-${{ matrix.python-version }}
          path: |
            .env
            requirements.txt
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 3: SMOKE TESTS (Fast feedback)
  # ═══════════════════════════════════════════════════════════════════════════
  
  smoke-tests:
    name: 💨 Smoke Tests
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: 📥 Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: build-3.12
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps chromium
      
      - name: 💨 Run Smoke Tests
        id: smoke
        run: |
          # Run smoke tests and capture output
          pytest tests/ -v -m "smoke" --tb=short --maxfail=3 --junitxml=smoke-results.xml 2>&1 | tee smoke-output.txt
        continue-on-error: true
      
      - name: 📋 Generate Smoke Test Summary
        if: always()
        run: |
          echo "## 💨 Smoke Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Extract test counts
          PASSED=$(grep -oP '\d+(?= passed)' smoke-output.txt | head -1 || echo "0")
          FAILED=$(grep -oP '\d+(?= failed)' smoke-output.txt | head -1 || echo "0")
          # Ensure FAILED is never empty
          FAILED=${FAILED:-0}
          PASSED=${PASSED:-0}
          TOTAL=$((PASSED + FAILED))
          
          # Status badge
          if [ "$FAILED" = "0" ] && [ "$PASSED" != "0" ]; then
            echo "### ✅ All Smoke Tests Passed!" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ⚠️ No Smoke Tests Found" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| ✅ Passed | $PASSED |" >> $GITHUB_STEP_SUMMARY
          echo "| ❌ Failed | $FAILED |" >> $GITHUB_STEP_SUMMARY
          echo "| 📊 Total | $TOTAL |" >> $GITHUB_STEP_SUMMARY
          
          # Extract duration
          DURATION=$(grep -oP '\d+\.\d+s' smoke-output.txt | tail -1 || echo "N/A")
          echo "| ⏱️ Duration | $DURATION |" >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "<details>" >> $GITHUB_STEP_SUMMARY
          echo "<summary>📜 View Test Details</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          grep -E "PASSED|FAILED|ERROR" smoke-output.txt | head -20 >> $GITHUB_STEP_SUMMARY || echo "No test results found" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 4: FULL TEST SUITE
  # ═══════════════════════════════════════════════════════════════════════════
  
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest
    needs: [build, smoke-tests]
    permissions:
      checks: write
      pull-requests: write
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
      fail-fast: false
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🐍 Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: 📥 Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: build-${{ matrix.python-version }}
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps chromium
      
      - name: 🧪 Run tests with pytest
        id: pytest
        run: |
          pytest tests/ -v \
            --tb=short \
            --maxfail=5 \
            --reruns 2 \
            --reruns-delay 1 \
            -n auto \
            --alluredir=./allure-results \
            --clean-alluredir \
            --junitxml=./test-results/junit.xml \
            --cov=. \
            --cov-report=xml:coverage.xml \
            --cov-report=html:coverage-report
        continue-on-error: true
      
      - name: 📊 Generate Allure Report
        if: always()
        run: |
          # Install Allure CLI (enforce HTTPS-only with TLS 1.2+)
          curl --proto "=https" --tlsv1.2 -sSf -L -o allure-2.25.0.tgz https://github.com/allure-framework/allure2/releases/download/2.25.0/allure-2.25.0.tgz
          tar -zxvf allure-2.25.0.tgz
          sudo mv allure-2.25.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure
          
          # Generate report
          allure generate ./allure-results -o ./allure-report --clean
      
      - name: 📅 Generate Artifact Metadata
        if: always()
        run: |
          CREATED_AT=$(TZ='Asia/Dhaka' date '+%d/%m/%Y %H:%M:%S')
          echo "Created: $CREATED_AT" > ./allure-results/artifact-info.txt
          echo "Created: $CREATED_AT" > ./allure-report/artifact-info.txt
          echo "Created: $CREATED_AT" > ./test-results/artifact-info.txt 2>/dev/null || mkdir -p ./test-results && echo "Created: $CREATED_AT" > ./test-results/artifact-info.txt
          echo "ARTIFACT_TIMESTAMP=$CREATED_AT" >> $GITHUB_ENV
          echo "📅 Artifact Created: $CREATED_AT" >> $GITHUB_STEP_SUMMARY
      
      - name: 📤 Upload Allure Results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: allure-results-${{ matrix.python-version }}-${{ github.run_number }}
          path: ./allure-results
          retention-days: 30
      
      - name: 📤 Upload Allure Report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: allure-report-${{ matrix.python-version }}-${{ github.run_number }}
          path: ./allure-report
          retention-days: 30
      
      - name: 📤 Upload Test Results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: test-results-${{ matrix.python-version }}-${{ github.run_number }}
          path: ./test-results
          retention-days: 30
      
      - name: 📤 Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: coverage-report-${{ matrix.python-version }}-${{ github.run_number }}
          path: |
            coverage.xml
            coverage-report/
          retention-days: 30
      
      - name: 📈 Publish Test Results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@27d65e188ec43221b20d26de30f4892fad91df2f  # v2
        with:
          files: ./test-results/junit.xml
          check_name: Test Results (Python ${{ matrix.python-version }})
      
      - name: ❌ Fail job if tests failed
        if: steps.pytest.outcome == 'failure'
        run: exit 1

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 5: SONARCLOUD CODE ANALYSIS
  # ═══════════════════════════════════════════════════════════════════════════
  
  sonarcloud:
    name: 🔬 SonarCloud Analysis
    runs-on: ubuntu-latest
    needs: test
    if: always()
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          fetch-depth: 0  # Full history for better analysis
      
      - name: 📥 Download coverage report
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: coverage-report-3.12-${{ github.run_number }}
          path: .
        continue-on-error: true
      
      - name: 🔬 SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@fd88b7d7ccbaefd23d8f36f73b59db7a3d246602  # v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.python.coverage.reportPaths=coverage.xml
      
      - name: 📋 SonarCloud Summary
        run: |
          echo "## 🔬 SonarCloud Analysis Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 **View Results:** [SonarCloud Dashboard](https://sonarcloud.io/project/overview?id=foyezkabir_playwright-api-automation-python)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Metrics Analyzed:" >> $GITHUB_STEP_SUMMARY
          echo "- 📊 Code Coverage" >> $GITHUB_STEP_SUMMARY
          echo "- 🐛 Bugs & Vulnerabilities" >> $GITHUB_STEP_SUMMARY
          echo "- 🔒 Security Hotspots" >> $GITHUB_STEP_SUMMARY
          echo "- 💨 Code Smells" >> $GITHUB_STEP_SUMMARY
          echo "- 📈 Duplications" >> $GITHUB_STEP_SUMMARY

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 6: COVERAGE REPORT
  # ═══════════════════════════════════════════════════════════════════════════
  
  coverage:
    name: 📈 Coverage Report
    runs-on: ubuntu-latest
    needs: test
    if: always()
    
    steps:
      - name: 📥 Download coverage report
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: coverage-report-3.12-${{ github.run_number }}
          path: ./coverage
      
      - name: 📊 Generate Coverage Summary
        run: |
          echo "## 📈 Test Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f coverage/coverage.xml ]; then
            # Extract coverage percentage from XML
            COVERAGE=$(grep -oP 'line-rate="\K[0-9.]+' coverage/coverage.xml | head -1)
            COVERAGE_PCT=$(echo "$COVERAGE * 100" | bc | cut -d'.' -f1)
            
            # Determine badge color
            if [ "$COVERAGE_PCT" -ge 80 ]; then
              BADGE="🟢"
              STATUS="Excellent"
            elif [ "$COVERAGE_PCT" -ge 60 ]; then
              BADGE="🟡"
              STATUS="Good"
            elif [ "$COVERAGE_PCT" -ge 40 ]; then
              BADGE="🟠"
              STATUS="Fair"
            else
              BADGE="🔴"
              STATUS="Needs Improvement"
            fi
            
            echo "### $BADGE Overall Coverage: ${COVERAGE_PCT}% ($STATUS)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extract file-level coverage
            echo "| File | Coverage |" >> $GITHUB_STEP_SUMMARY
            echo "|------|----------|" >> $GITHUB_STEP_SUMMARY
            
            # Parse coverage.xml using Python for reliable XML parsing
            python3 -c "
          import xml.etree.ElementTree as ET
          try:
              tree = ET.parse('coverage/coverage.xml')
              root = tree.getroot()
              for package in root.findall('.//package'):
                  for cls in package.findall('.//class'):
                      filename = cls.get('filename', 'unknown')
                      line_rate = float(cls.get('line-rate', 0))
                      pct = int(line_rate * 100)
                      icon = '✅' if pct >= 80 else ('⚠️' if pct >= 50 else '❌')
                      print(f'| {icon} {filename} | {pct}% |')
          except Exception as e:
              print(f'| (Error: {e}) | - |')
          " >> $GITHUB_STEP_SUMMARY
            
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 [View Full HTML Report](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/coverage-report/index.html)" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Coverage report not found" >> $GITHUB_STEP_SUMMARY
          fi

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 7: DEPLOY REPORTS
  # ═══════════════════════════════════════════════════════════════════════════
  deploy:
    name: 🚀 Deploy Reports
    runs-on: ubuntu-latest
    needs: [test, coverage, sonarcloud]
    if: always() && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 📁 Create publish directory
        run: mkdir -p publish
      
      - name: 📥 Download Allure Report (Python 3.12)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: allure-report-3.12-${{ github.run_number }}
          path: ./publish/allure-report
      
      - name: 📥 Download Coverage Report (Python 3.12)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: coverage-report-3.12-${{ github.run_number }}
          path: ./publish/coverage-report
        continue-on-error: true
      
      - name: 🏠 Create root redirect page
        run: |
          REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure-report/index.html"
          cat > publish/index.html << EOF
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>Redirecting to Allure Report...</title>
            <meta http-equiv="refresh" content="0; url=${REPORT_URL}">
            <script>
              window.location.href = "${REPORT_URL}";
            </script>
          </head>
          <body>
            <p>Redirecting to <a href="${REPORT_URL}">Allure Report</a>...</p>
          </body>
          </html>
          EOF
      
      - name: 🌐 Publish Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e  # v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./publish
          publish_branch: gh-pages
          force_orphan: true
      
      - name: 📋 Report URL in Summary
        run: |
          echo "## 📊 Reports Published!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Report | Link |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 📈 **Allure Report** | [View Report](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure-report) |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔬 **SonarCloud** | [View Analysis](https://sonarcloud.io/project/overview?id=foyezkabir_playwright-api-automation-python) |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Reports will be available in ~2 minutes after deployment completes." >> $GITHUB_STEP_SUMMARY

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 7: NOTIFICATIONS
  # ═══════════════════════════════════════════════════════════════════════════
  
  notify:
    name: 📢 Notify
    runs-on: ubuntu-latest
    needs: [code-quality, type-check, build, smoke-tests, test, sonarcloud, coverage, deploy, security-scan]
    if: always()
    
    steps:
      - name: 📊 Prepare Notification Summary
        id: summary
        run: |
          if [ "${{ needs.test.result }}" == "success" ]; then
            echo "status=✅ Success" >> $GITHUB_OUTPUT
            echo "color=good" >> $GITHUB_OUTPUT
          elif [ "${{ needs.test.result }}" == "failure" ]; then
            echo "status=❌ Failed" >> $GITHUB_OUTPUT
            echo "color=danger" >> $GITHUB_OUTPUT
          else
            echo "status=⚠️ ${{ needs.test.result }}" >> $GITHUB_OUTPUT
            echo "color=warning" >> $GITHUB_OUTPUT
          fi
      
      - name: 📋 Create GitHub Summary
        run: |
          echo "## 📢 Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🎨 Code Quality | ${{ needs.code-quality.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📊 Type Check | ${{ needs.type-check.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔨 Build | ${{ needs.build.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 💨 Smoke Tests | ${{ needs.smoke-tests.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🧪 Tests | ${{ needs.test.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔬 SonarCloud | ${{ needs.sonarcloud.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📈 Coverage | ${{ needs.coverage.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🚀 Deploy | ${{ needs.deploy.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔒 Security | ${{ needs.security-scan.result || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Overall Status:** ${{ steps.summary.outputs.status }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Reports" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 **Allure Report:** https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure-report" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 **SonarCloud:** https://sonarcloud.io/project/overview?id=foyezkabir_playwright-api-automation-python" >> $GITHUB_STEP_SUMMARY
      
      - name: 📨 Send Slack Notification
        if: always()
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e  # v3
        with:
          status: custom
          custom_payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "${{ needs.test.result == 'success' && '✅' || '❌' }} API Tests - ${{ needs.test.result }}",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {"type": "mrkdwn", "text": "*Repository:*\n${{ github.repository }}"},
                    {"type": "mrkdwn", "text": "*Branch:*\n${{ github.ref_name }}"},
                    {"type": "mrkdwn", "text": "*Run:*\n#${{ github.run_number }}"},
                    {"type": "mrkdwn", "text": "*Author:*\n${{ github.actor }}"}
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*📊 Reports:*\n• <https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure-report|📈 Allure Report>\n• <https://sonarcloud.io/project/overview?id=foyezkabir_playwright-api-automation-python|🔬 SonarCloud>"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}

  # ═══════════════════════════════════════════════════════════════════════════
  # STAGE 9: CLEANUP
  # ═══════════════════════════════════════════════════════════════════════════
  
  cleanup:
    name: 🧹 Cleanup
    runs-on: ubuntu-latest
    needs: [notify]
    if: always()
    permissions:
      actions: write
    
    steps:
      - name: 🗑️ Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@5bf9a1dac5c4d041c029f0a8370ddf0c5cb5aeb7  # v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 30
          keep_minimum_runs: 10
        continue-on-error: true
      
      - name: 📋 Cleanup Summary
        run: |
          echo "## 🧹 Cleanup Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Old workflow runs older than 30 days deleted" >> $GITHUB_STEP_SUMMARY
          echo "- Minimum 10 recent runs retained" >> $GITHUB_STEP_SUMMARY

  # ═══════════════════════════════════════════════════════════════════════════
  # PARALLEL: SECURITY SCAN
  # ═══════════════════════════════════════════════════════════════════════════
  security-scan:
    name: 🔒 Security Dependency Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      
      - name: 🔍 Run Safety check
        run: |
          pip install safety
          safety check --json || true
